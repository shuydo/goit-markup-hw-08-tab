Смутное время - Ария похоже на Рамм
------------------------------------
! Нет первой картинки портфолио для гаджетов без оверлея

есть ещё совпадения по файлам и КОМПОНЕНТАМ?

В меню навигации не раб. подсветки пунктов в Моби, да и в Таблете

с телефона не видны иконки свг в обоих браузерах
-------------------------------------
почему высота чекбокса+текст=больше высоты каждогоиз них? 

при переходе с DTtoT картинки в команде уменьшаюся, но после F5 становятся норм.

в макете (таблет) непонятно: строка 24рх, а блок в кот. она стоит 21рх?
(поэтому и высота адрес-контейнера у меня не сходится)

цвет иконок в футере вроде серый брался из макет, почему теперь в макете белый? 

размер шрифта phone & email 12,14,18 скачет почему (footer)?

Объединить спрайты и минимизировать

в клиентах маржин не 30!

кнопка Подписаться в Моби больше, чем в макете

	(конверт и смартфон не отцентрованы по высоте с почтой и тел.номером)
всё ещё не исправлена ломанная на фокусе (см.фон и конверт, футер также)

всё же кнопка заказа услуги синяя!

	крест закрытия меню должен синеть? 
	(глупость в мобиверсии - под пальцем его изменение никто не увидит?

	кнопка закрытия модалки вне модалки

	(после снятия чека) исчезла рамка (мобила)

кнопка модалки "Отправить" при акт.меняет оттенок (с 2196F3 на 188CE8)

нет подчёркивания акт страницы(мобила)

border: 1px solid #ECECEC; под шапкой (мобила)

крест закрытия меню не синеет + аутлайн активности?

нет анимаций при применении модального окна, 
	на иконках клиентов

убрать черный аутлайн с кнопок при фокусе

кнопка Все в потфолио всегда должна быть активна?

со шрифтами и подсветками в футере не порядок (мобила)
-----------------------------
Tablet
--------------------
в контактах шапки: флекс - эраунд, а не зазор в 10рх

цвет навигации - фиолетовый, с чего бы?

Почему то join-header не подчиняется центровке, а label да?